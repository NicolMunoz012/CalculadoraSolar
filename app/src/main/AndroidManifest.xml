<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SolarCalculatorApp"
        tools:targetApi="31">
        
        <!-- Actividad principal - Entrada de datos -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de resultados -->
        <activity
            android:name=".ui.ResultadosActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Actividad de configuración -->
        <activity
            android:name=".ui.ConfiguracionActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.ExplicacionCalculosActivity"
            android:label="Explicación de Cálculos"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/Theme.SolarCalculatorApp" />
    </application>

</manifest>
